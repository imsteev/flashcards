# install postgres on your machine.
# if using brew, this will install handy binaries that make calls to libpq.
brew install postgresql@14

# start postgres
brew services start postgresql@14

# create a database with <name> using the default db (localhost, no pw, logged in user (???) )
createdb <db-name>

# interactive postgres shell
psql <db-name>

questions to answer

* how do i know where my postgres service is running on?
* how do i know what the postgres url is for a certain db?

handy diagram from prisma docs lol:

postgres[ql]://[username[:password]@][host[:port],]/database[?parameter_list]

\_____________/\____________________/\____________/\_______/\_______________/
     |                   |                  |          |            |
     |- schema           |- userspec        |          |            |- parameter list
                                            |          |
                                            |          |- database name
                                            |
                                            |- hostspec
